const navigationBarSections = document.querySelectorAll(
  ".navigation-bar-sections"
);

const skillImages = document.querySelectorAll(".skill-image");

const projectContainer = document.querySelectorAll(".project");

const skillImagesArray = [...skillImages];
console.log(skillImagesArray);
